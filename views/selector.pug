extends layout

block content
  h1= title
  - var image_names = ["pancake1.jpg","pancake2.jpg","pancake3.jpg","pancake4.jpg","pancake5.jpg"];
  - for(let i=0; i<3; i++){
    - let number = Math.floor(Math.random()*5);
    img(src = `/images/${image_names[number]}` alt = `Image No ${image_names[number]}`)
    -}